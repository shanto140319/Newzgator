# Public browser API origin (required for client load-more / save / react).
NEXT_PUBLIC_ARTICLE_API_BASE_URL=https://newzgator-api.onrender.com
# Optional server override; falls back to NEXT_PUBLIC_ARTICLE_API_BASE_URL.
ARTICLE_API_BASE_URL=https://newzgator-api.onrender.com
# Set this to the real public origin in production for canonical and sharing URLs.
SITE_URL=http://localhost:3000
